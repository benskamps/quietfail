"""quietfail — a linter for failures that pass every check you already run.

The unifying property of every class it looks for: the failure is invisible in
exactly the surfaces you would check. Green CI, exit 0, output on schedule,
and nothing making progress.
"""

__version__ = "0.2.0"
